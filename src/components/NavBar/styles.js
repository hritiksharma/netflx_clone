import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  toolbar: {
    background: "red",
    height: "80px",
    display: "flex",
    justifyContent: "center",
    marginLeft: "240px",
  },
}));
